# Library-Book-Request-Management-System
A ServiceNow application that allows students to request library books online. It uses a custom table to manage requests, validates that the issue date is not in the past, automatically generates a unique request number, and notifies the librarian when a request is submitted.
